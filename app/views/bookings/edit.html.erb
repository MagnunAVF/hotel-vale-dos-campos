<h1 class="ui horizontal divider header">
  Editar Reserva
</h1>
<br/><br/>
<div class="ui container">
  <div class="ui center aligned grid">
    <%= render 'form', booking: @booking %>
    </br>
    <% if current_user.type == "Employee" %>
      <%= link_to 'Voltar', bookings_path %>
    <% end %>
    </br></br>
  </div>
</div>
