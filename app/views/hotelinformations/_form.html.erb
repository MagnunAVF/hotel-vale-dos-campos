<%= form_for(hotelinformation) do |f| %>
  <% if hotelinformation.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(hotelinformation.errors.count, "error") %> prohibited this hotelinformation from being saved:</h2>

      <ul>
      <% hotelinformation.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :description %>
    <%= f.text_field :description %>
  </div>

  <div class="field">
    <%= f.label :email %>
    <%= f.text_field :email %>
  </div>

  <div class="field">
    <%= f.label :fone %>
    <%= f.text_field :fone %>
  </div>

  <div class="field">
    <%= f.label :address %>
    <%= f.text_field :address %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
